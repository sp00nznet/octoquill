org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Your GitHub OAuth App client ID, for "Sign in with GitHub" (device flow).
# Create one at https://github.com/settings/developers -> New OAuth App -> tick "Enable Device Flow".
# Leave blank and the app falls back to pasting a personal access token.
octoquill.clientId=
